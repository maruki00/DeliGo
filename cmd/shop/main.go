package main

func main() {
	// instance, cleanup, err := app.Init()
	// if err != nil {
	// 	log.Fatalf("failed to initialize shop service: %v", err)
	// }
	// defer cleanup()

	// router := api.NewRouter(instance)
	// log.Printf("shop service initialized: %s", instance.Health())
	// if err := router.Run(":8090"); err != nil {
	// 	log.Fatalf("failed to serve shop service: %v", err)
	// }
}
